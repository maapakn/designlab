<h1>Editando Trabajo</h1>

<%= form_for(@trabajo, url: admin_trabajo_path(@trabajo), html: {method: "patch"}) do |f| %>
  <% if @trabajo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trabajo.errors.count, "error") %> prohibited this trabajo from being saved:</h2>

      <ul>
      <% @trabajo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
      <%= f.label :category_id, 'Categoria' %><br>
      <%= collection_select(:trabajo, :category_id, Spree::Category.all, :id, :nombre) %>
      <script src="multiple-select.js"></script>
              <script>
                  $("select").multipleSelect({
                      single: true,
                      multipleWidth: 55,
                      width: '100%'
                  });
              </script>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Mostrar', edit_admin_trabajo_path(@trabajo) %> |
<%= link_to 'Atras', admin_trabajos_path %>
